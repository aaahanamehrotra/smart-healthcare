import React from "react";
import styles from "../styles/Footer.module.css";

const Feature = ({ number, title, desc }) => {
  return <div className={styles.footer}>PulmoGen 2023</div>;
};

export default Feature;
